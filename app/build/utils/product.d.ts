/**
 * Computes product of an array (using compensated product method)
 *
 * @param {Array<number>} array Array.
 * @returns {number} Product.
 */
export default function product(array: Array<number>): number;
